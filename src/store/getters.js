export const getUnit = state => {
  return state.unit
}

export const getEcoScore = state => {
  return state.ecoScore
}

export const getTrip = state => {
  return state.trip
}

export const getDrivingStatus = state => {
  return state.drivingStatus
}

export const getStackData = state => {
  return state.stackTripData
}
